# pnpm configuration for deployment compatibility
# Use hoisted node_modules structure instead of symlinks
# This fixes module resolution issues in deployment environments like Render
node-linker=hoisted

# Automatically install peer dependencies
auto-install-peers=true

# Optionally enable strict peer dependencies
# strict-peer-dependencies=false
